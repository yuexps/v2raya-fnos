#!/bin/bash

# This script is called before the user installs the application.

# 获取用户选择的端口
PORT="${wizard_port:-2017}"

# 校验端口是否被占用
if ss -tuln 2>/dev/null | grep -q ":${PORT} "; then
    echo "Error: Port ${PORT} is already in use" > "${TRIM_TEMP_LOGFILE}"
    exit 1
fi

exit 0
